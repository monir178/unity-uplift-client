import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    reliefs: [],
}

const reliefSlice = createSlice({
    name: 'relief',
    initialState,
    reducers: {

    }
})

export default reliefSlice.reducer;